apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

components:
  - qts
  - organisation

patches:
  - path: patch.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: argocd-repo-server
